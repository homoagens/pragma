fastapi
uvicorn[standard]
requests
rich
# The chat prompt: live completion of the / commands, plus line editing
# and history. Pure Python, no build step.
prompt_toolkit
ddgs
python-dotenv
json-repair
# ripgrep binary for grep_search. Optional in practice: the skill falls back to
# a pure-Python walk if the wheel has no build for the platform.
ripgrep
